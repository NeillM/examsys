# This is a sample build configuration for PHP.
# Check our guides at https://confluence.atlassian.com/x/e8YWN for more examples.
# Only use spaces to indent your .yml configuration.
# -----
# You can specify a custom docker image from Docker Hub as your build environment.
image: php:7.2.3

pipelines:
  default:
    - step:
        caches:
          - composer
        script:
          - apt-get update && apt-get install -y unzip gnupg
          - curl -sL https://deb.nodesource.com/setup_8.x | bash -
          - apt-get install -y nodejs
          - curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer
          - composer install --no-dev
          - npm install --production
          - cd ..
          - branch=${BITBUCKET_BRANCH#*/}
          - tar --exclude='.git' --exclude='.gitignore' --exclude='Gruntfile.js' --exclude='Vagrantfile' --exclude='bitbucket-pipelines.yml' --exclude='composer.phar' --exclude='crowdin.yml.example' --exclude='config/behat.example.xml' --exclude='config/phpunit.example.xml' --exclude='testing/behat' --exclude='testing/datagenerator' --exclude='testing/eslint' --exclude='testing/javascript' --exclude='testing/unittest' -czvf rogo-${branch}.tar.gz -C build .
          - curl -X POST "https://${BB_AUTH_STRING}@api.bitbucket.org/2.0/repositories/${BITBUCKET_REPO_OWNER}/${BITBUCKET_REPO_SLUG}/downloads" --form files=@"rogo-${branch}.tar.gz"